{
  "name": "raak",
  "version": "0.0.2",
  "private": false,
  "license": "MIT",
  "repository": "https://github.com/cellajs/raak",
  "description": "Issue tracker for teams that work on multiple projects. High density. Fast. Fun.",
  "keywords": [
    "issue tracker",
    "project management",
    "open-source",
    "cellajs"
  ],
  "author": "CellaJS <info@cellajs.com>",
  "homepage": "https://raak.dev",
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "bench": "pnpm -C bench bench",
    "build": "pnpm -r --parallel clean && pnpm -r --parallel build",
    "build:dev": "pnpm -r --parallel build:dev",
    "build:staging": "pnpm -r --parallel build:staging",
    "cella": "cella",
    "check": "pnpm sdk && pnpm --filter backend ts && pnpm -r --filter '!backend' ts && pnpm --filter shared lens:check && pnpm lint:fix",
    "clean": "rimraf backend/dist frontend/dist frontend/stats",
    "dev": "pnpm clean && cross-env pnpm --reporter=append-only -r --parallel --stream dev",
    "dev:migration": "pnpm clean && cross-env DATABASE_URL=postgres://postgres:postgres@0.0.0.0:5555/postgres DATABASE_ADMIN_URL=postgres://postgres:postgres@0.0.0.0:5555/postgres pnpm --reporter=append-only -r --parallel --stream dev",
    "dev:proxy": "pnpm clean && cross-env pnpm --reporter=append-only -r --parallel --stream --filter backend --filter cdc-worker --filter frontend --filter proxy dev",
    "dev:single": "pnpm clean && cross-env SINGLE_VM=true pnpm --reporter=append-only -r --parallel --stream --filter backend --filter frontend dev",
    "docker": "pnpm --filter backend docker:up --detach",
    "docker:test": "docker compose -f backend/compose.yaml --profile test up -d --wait db_test",
    "docker:test:down": "docker compose -f backend/compose.yaml --profile test down",
    "generate": "pnpm --filter backend generate",
    "infra": "pnpm infra:compose && pnpm --filter infra cli",
    "infra:compose": "pnpm --filter infra compose:generate",
    "lint": "pnpm biome check .",
    "lint:fix": "pnpm biome check --write --unsafe",
    "offline": "cross-env pnpm -r --parallel --stream --filter backend --filter cdc-worker dev & pnpm --filter frontend offline",
    "sdk": "pnpm --filter backend generate:openapi && pnpm --filter sdk generate:sdk",
    "seed": "pnpm --filter backend seed",
    "session": "bash session.sh",
    "start": "pnpm -r --parallel start",
    "stop": "pnpm -r --parallel --stream stop",
    "story": "pnpm --filter frontend storybook",
    "test": "pnpm test:full",
    "test:core": "pnpm docker:test && cross-env TEST_MODE=core vitest run --silent=passed-only",
    "test:core:verbose": "pnpm docker:test && cross-env TEST_MODE=core vitest run",
    "test:full": "pnpm docker:test && cross-env TEST_MODE=full COVERAGE_REPORTERS=summary vitest run --coverage.enabled --silent=passed-only",
    "test:full:verbose": "pnpm docker:test && cross-env TEST_MODE=full vitest run",
    "test:storybook": "pnpm --filter frontend exec vitest run --project=storybook",
    "test:update": "pnpm docker:test && cross-env TEST_MODE=core vitest run -u",
    "ts": "pnpm -r ts",
    "tsperf": "pnpm --filter backend tsperf && pnpm --filter frontend tsperf && pnpm --filter cdc-worker tsperf && pnpm --filter yjs-worker tsperf",
    "tunnel": "pnpm clean && cross-env pnpm --reporter=append-only -r --parallel --stream tunnel"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.2",
    "@cellajs/cli": "^0.0.5",
    "@commitlint/cli": "^21.2.0",
    "@commitlint/config-conventional": "^21.2.0",
    "@evilmartians/lefthook": "^2.1.9",
    "@typescript/native-preview": "7.0.0-dev.20260701.1",
    "cross-env": "^10.1.0",
    "knip": "^6.24.0",
    "rimraf": "^6.1.3",
    "semver": "^7.7.3",
    "tsx": "^4.23.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  },
  "packageManager": "pnpm@11.8.0",
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ],
    "rules": {
      "type-enum": [
        2,
        "always",
        [
          "feat",
          "fix",
          "perf",
          "refactor",
          "revert",
          "docs",
          "chore",
          "build",
          "ci",
          "style",
          "test"
        ]
      ],
      "header-max-length": [
        2,
        "always",
        120
      ],
      "body-max-line-length": [
        0,
        "always",
        200
      ],
      "footer-max-line-length": [
        0,
        "always",
        200
      ]
    }
  }
}
