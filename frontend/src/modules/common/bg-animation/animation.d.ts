/** Starts the animated background cells. */
export function start_cells(canvas: HTMLCanvasElement): void;
/** Stops the animated background cells. */
export function stop_cells(): void;
/** Updates the color of the animated background cells. */
export function set_cell_color(color: [number, number, number]): void;
