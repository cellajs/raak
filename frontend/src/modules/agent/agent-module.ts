import { registerModule } from 'shared/module-registry';

registerModule({
  name: 'agent',
  owner: 'app',
  scope: ['frontend'],
  description: 'AI chat UI built on top of the backend AI capability layer, providing an interactive assistant panel.',
});
